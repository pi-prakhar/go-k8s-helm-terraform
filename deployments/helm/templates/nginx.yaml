apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}-ingress
  namespace: {{ .Values.namespace }}
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          # Route for server1
          - path: /server1/test
            pathType: Prefix
            backend:
              service:
                name: {{ .Release.Name }}-server1
                port:
                  number: {{ .Values.service.server1Port }}

          # Route for server2
          - path: /server2/test
            pathType: Prefix
            backend:
              service:
                name: {{ .Release.Name }}-server2
                port:
                  number: {{ .Values.service.server2Port }}
